{
  "name": "loop-tests",
  "version": "0.0.1",
  "description": "Test execution time between different loop types",
  "main": "loop-tests.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Ferguson",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12.7.8"
  }
}
